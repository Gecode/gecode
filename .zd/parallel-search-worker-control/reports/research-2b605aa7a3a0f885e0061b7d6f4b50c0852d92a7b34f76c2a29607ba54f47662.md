# Research report: parallel-search-worker-control

## Question

> Verify issue parallel-search-worker-control-011: Validate the Origin and Variant API migration

## Summary

> Yes, the amended lineage is sufficient to treat issue 011's technical acceptance criteria as satisfied and proceed to its normal completion transition. The issue records that issue 012 supplied the durable validation artifacts and is complete, and it names an authoritative independent verifier that passed all five acceptance criteria. The recorded validation covers the required compile rejection/override checks, RBS and all PBS shapes in threaded and thread-disabled configurations, migration-sensitive lifecycle and callback behavior, built examples and documentation, the terminology gate, and the complete threaded Search suite. This conclusion is about the evidence lineage recorded in the captured issue; it does not independently re-audit the retained report or audit file, whose bytes are not in this packet. The issue remains procedurally open with unchecked boxes, so zdev must still confirm dependency state, check the boxes through its completion flow, and run the prescribed `zd issues check parallel-search-worker-control` gate.

## Findings

### The acceptance boundary is explicit and remains unchanged

> The five unchecked criteria are the requirements to satisfy; the amendment adds evidence lineage rather than narrowing those requirements.

Evidence:
- `.zd/parallel-search-worker-control/issues/011-validate-the-origin-and-variant-api-migration.md` — > ## Acceptance criteria
> 
> - [ ] Compile tests fail if the old Space hook names return or if the new signatures cannot be overridden with override.
> - [ ] Behavioral tests cover both Origin and Variant callbacks in RBS, single-asset PBS, sequential multi-asset PBS, and parallel PBS.
> - [ ] Tests demonstrate that RBS return-value semantics and PBS ignored-return semantics have not accidentally changed during the rename.
> - [ ] The migration example builds against the Gecode 7 headers and gives users an exact old-to-new override mapping.
> - [ ] Every remaining master/slave occurrence in the maintained Gecode source and documentation is reviewed and recorded, with no stale meta-search terminology left behind.

### Issue 012 is recorded as the completed implementation boundary

> The amended issue says issue 012 added the durable artifacts needed by issue 011, identifies the corrective commit and collection run, and records zdev integration after independent verification.

Evidence:
- `.zd/parallel-search-worker-control/issues/011-validate-the-origin-and-variant-api-migration.md` — > Issue `parallel-search-worker-control-012` added the durable validation artifacts
> required by this issue and is complete. Its corrective implementation is commit
> `c1dba83d0c34cf1c1dccffd86d419f7db599308f`, collected by run
> `rework-parallel-search-worker-control-012-41f8fc58c673` and integrated through
> zdev after independent verification.

### The authoritative verifier is recorded as passing every criterion

> The strongest affirmative statement in the packet is that the named verifier reconstructed the collected patch on its recorded base and passed all five acceptance criteria. That directly binds the completed implementation evidence to issue 011's full acceptance boundary.

Evidence:
- `.zd/parallel-search-worker-control/issues/011-validate-the-origin-and-variant-api-migration.md` — > The authoritative verifier run is
> `verify-parallel-search-worker-control-012-901a2bbc618a`. It passed all five
> acceptance criteria after reconstructing the collected patch on its recorded
> base.

### Recorded checks cover compilation, callback behavior, examples, documentation, and terminology

> The verifier summary maps to the required positive and negative API checks, RBS/PBS behavioral matrix, lifecycle-sensitive callbacks, migration example and documentation builds, and terminology review. Its explicit claim of passing all five criteria supplies the return-semantics conclusion; the bullet list describes the exercised checks without replacing the criteria.

Evidence:
- `.zd/parallel-search-worker-control/issues/011-validate-the-origin-and-variant-api-migration.md` — > Its recorded validation includes:
> 
> - independent positive and negative compilation checks for the current and
>   removed hooks;
> - repeated threaded and thread-disabled MetaDispatch scenarios for RBS and
>   every PBS shape;
> - migration-sensitive no-good, reset, ownership, stop, and BAB incumbent
>   propagation checks;
> - CMake and Autoconf example, documentation, terminology, and `check` targets;

### Broad Search validation and the known thread-disabled baseline are recorded

> The lineage records a passing complete threaded Search suite. For the thread-disabled configuration it records an exact match to twelve pre-existing PBS expectation failures, which supports no migration regression without misreporting that configuration as wholly green.

Evidence:
- `.zd/parallel-search-worker-control/issues/011-validate-the-origin-and-variant-api-migration.md` — > - a passing complete threaded Search suite; and
> - an exact before-and-after match for the twelve pre-existing thread-disabled
>   PBS expectation failures.

### The migration validation is consistent with the broader RBS/PBS boundary

> The area brief requires RBS and PBS to preserve and transport the control handle, so validating their public hook migration is relevant supporting work rather than an unrelated rename exercise.

Evidence:
- `.zd/parallel-search-worker-control/brief.md` — > - The first version targets parallel DFS and BAB. RBS and PBS must preserve and transport the control handle, but they do not choose allocation policy.

### Completion still requires the recorded zdev gate

> The evidence supports technical acceptance, but the captured issue remains open and its validation section requires the issue check after evidence is linked. Completion should use the normal zdev transition rather than treating this research result as the transition itself.

Evidence:
- `.zd/parallel-search-worker-control/issues/011-validate-the-origin-and-variant-api-migration.md` — > status = "open"
> blocked_by = ["parallel-search-worker-control-010", "parallel-search-worker-control-012"]
- `.zd/parallel-search-worker-control/issues/011-validate-the-origin-and-variant-api-migration.md` — > - Run `zd issues check parallel-search-worker-control` after all migration evidence is linked.

## Limitations

- The retained verifier report and source-backed audit are named in the issue but were not captured as source bytes in this packet. This result verifies the issue's amended lineage statements; it does not independently inspect those two artifacts.
- The packet does not include current zdev status for issues 010 or 012. Although the issue text says 012 is complete, the completion command should confirm both blockers are satisfied in authoritative state.
- The acceptance checkboxes and issue status remain unchanged in the captured source. Research can support completion but cannot itself perform or prove the zdev state transition.

## Recommendations

- Run `zd issues check parallel-search-worker-control` and inspect current blocker status before completing issue 011.
- Use the normal zdev completion transition to bind the named verifier run and retained artifacts, mark all five acceptance criteria satisfied, and change the issue from open to done.
- Preserve the exact qualification for the thread-disabled suite: its twelve PBS expectation failures matched the pre-existing baseline; do not summarize that configuration as an unqualified full-suite pass.
