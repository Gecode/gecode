# Research report: parallel-search-worker-control

## Question

> Research issue parallel-search-worker-control-001: Specify the parked-worker scheduler state machine

## Summary

> Yes. The user-approved scheduler design satisfies all four acceptance criteria. It separates logical ownership from execution-slot permission, preserves `n_busy` as the exhaustion counter, guarantees eventual admission of a parked final owner through owner-prioritized round-robin lease handoff, defines lifecycle wakeups and a non-nested lock order for C_WORK, C_WAIT, C_RESET, and C_TERMINATE, and selects one sticky `Support::Event` per worker after comparing the viable primitives. This establishes the reviewed implementation boundary; it does not claim that the later scheduler implementation or benchmark work is complete.

## Findings

### Ownership and execution permission are independent

> Parking removes only the execution lease and preserves the worker's logical search state. `n_busy` continues to count workers that have not declared themselves idle, including parked owners.

Evidence:
- `.zd/parallel-search-worker-control/issues/001-specify-the-parked-worker-scheduler-state-machine.md` — > Logical search ownership and permission to execute are independent state axes.
> A worker is logically an owner while its current Space or path retains work,
> pending while it has not finished its empty-state transition, and idle only
> after reporting exhaustion. Independently, it either holds an execution-slot
> lease or does not. Parking removes only the lease: it does not set the worker
> idle, clear its Space or path, discard statistics or BAB state, or decrement
> `n_busy`.
- `.zd/parallel-search-worker-control/issues/001-specify-the-parked-worker-scheduler-state-machine.md` — > `n_busy` remains the exhaustion counter. It counts workers that have not
> declared themselves idle, including parked owners. Consequently,
> `n_busy == 0` retains its existing meaning: all construction-time workers have
> accounted for their local work.

### Lease migration gives a parked final owner progress

> A failed thief cannot retain the only execution lease while logical work remains. The finite owner-prioritized round-robin queue passes the lease until the remaining owner runs.

Evidence:
- `.zd/parallel-search-worker-control/issues/001-specify-the-parked-worker-scheduler-state-machine.md` — > | Active thief cannot steal while `n_busy > 0` | Hand its lease to the next parked non-idle worker, preferring owners and using round-robin order | Parked owner becomes active |
- `.zd/parallel-search-worker-control/issues/001-specify-the-parked-worker-scheduler-state-machine.md` — > Progress uses an owner-prioritized round-robin queue. While `C_WORK` remains
> active, the requested limit is at least one, and `n_busy > 0`, the scheduler
> maintains at least one lease or recorded handoff. An empty lease holder either
> steals work or passes the lease through the finite queue. Therefore a parked
> worker that owns the final non-stealable current branch is eventually
> reactivated.

### Command and lock protocols cover every lifecycle state

> Reconciliation occurs before the worker mutex, the scheduler mutex is never nested with existing search or lifecycle locks, and reset and termination wake every fixed barrier member.

Evidence:
- `.zd/parallel-search-worker-control/issues/001-specify-the-parked-worker-scheduler-state-machine.md` — > Slot reconciliation occurs at the top of a `C_WORK` iteration before acquiring
> the worker mutex. A lease authorizes one existing bounded loop action; resizing
> never interrupts propagation or recomputation. Scheduler state is protected by
> one scheduler mutex. That mutex is never nested with worker, search, reset,
> termination, or wait locks. Event waits and signals occur after releasing it.
> Updates to `n_busy` finish under the existing search lock before any scheduler
> handoff.
- `.zd/parallel-search-worker-control/issues/001-specify-the-parked-worker-scheduler-state-machine.md` — > During reset, after all start acknowledgements and while workers are blocked at
> the existing barrier, rebuild leases deterministically from the latest
> requested limit, including worker zero. `C_RESET` and `C_TERMINATE` wake every
> parked worker before waiting for acknowledgements. `C_WAIT` may leave already
> parked workers quiescent; a limit update during `C_WAIT` cannot admit search
> because the event predicate rechecks the command.

### Per-worker sticky events are the reviewed primitive

> The design compares shared and per-worker events, a new multi-waiter primitive, and a direct condition variable. It selects targeted per-worker events and obtains fairness from the queue.

Evidence:
- `.zd/parallel-search-worker-control/issues/001-specify-the-parked-worker-scheduler-state-machine.md` — > Use one existing sticky `Support::Event` per worker. Its single-waiter contract
> matches one event per fixed worker, retains signals issued before waiting, and
> permits targeted wakeups. A shared Event is invalid because it supports only
> one waiter. A new multi-waiter primitive enlarges the support-layer scope, and
> a direct condition variable would duplicate Gecode's conditional thread
> abstraction and require a new predicate-mutex protocol. Fairness comes from the
> explicit round-robin queue, not from the mutex or event.
- `.zd/parallel-search-worker-control/issues/001-specify-the-parked-worker-scheduler-state-machine.md` — > The user approved this design on 2026-07-24.

## Limitations

- This result proves the reviewed scheduler specification boundary, not its later implementation or measured overhead.
- The retained source-backed report is referenced by the issue but is not recaptured in this exact completion packet.

## Recommendations

- Complete issue 001 through zdev, then preserve the approved transition table, lock rule, and progress invariant in the implementation specification and tests.
