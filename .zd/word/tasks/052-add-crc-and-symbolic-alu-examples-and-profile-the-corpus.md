+++
schema_version = 1
id = "word-052"
key = "crc-alu-examples-profile"
area = "word"
status = "open"
blocked_by = ["word-051"]
+++
# Add CRC and symbolic ALU examples and profile the corpus

## Outcome

CRC-16/CCITT, reflected CRC-16/X-25, and a constructed symbolic ALU path become ordinary configurable examples, and profiling across all five realistic examples selects general native optimization work.

## Boundaries

- Add ordinary Script examples and normal inventories, not a benchmark runner, raw results, profiling framework, or production optimization.
- Keep quick defaults and explicit tens-of-seconds scales; do not manufacture runtime through repeated roots, output, or poor branching.
- Describe the ALU path as a constructed symbolic-execution-style model, not a paper instance; preserve CRC decomposition parity claims exactly.

## Done when

- [ ] Three licensed configurable examples model CRC-16/CCITT-FALSE, reflected CRC-16/X-25-style recurrence, and the constructed symbolic ALU path through public Word APIs with standard Script cloning/search and conditional Word build integration.
- [ ] Focused builds and modest runs verify stable semantics, while explicit retained configurations reproduce the prior one-root CRC and ALU scales without slowing ordinary example validation.
- [ ] Temporary Release profiling compares all five realistic examples, including exact native/Boolean CRC parity, and records counters, bounded samples, clone/recomputation sensitivity, memory, and operation-level hotspots.
- [ ] The Word brief records cross-workload conclusions and an exact dependency-ordered optimization task split, rejecting changes that are merely benchmark-specific or unsupported by measurements.

## Validation

- Build only the three new targets and run their modest defaults plus bounded retained configurations.
- Verify CRC Word/Boolean solutions, checksums, nodes, and failures exactly at the measured scale or a justified bounded adjacent scale.
- Run zd check word and git diff --check; repository changes remain examples, normal inventories, and zdev conclusions only.
