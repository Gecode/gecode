+++
schema_version = 1
id = "word-076"
key = "bounded-structural-mixed"
area = "word"
status = "done"
blocked_by = ["word-075"]
+++
# Measure and migrate structural and mixed Word constraints

## Outcome

Shifts, extensions, element, product_mod, and count/reduction surfaces exploit bounds only where independent measurements justify a native enhancement.

## Context

Implement stage six selectively; the word-070 matrix marks several surfaces as research rather than automatic migrations.

## Boundaries

- Rotations and Boolean logic remain cube operations unless new exact evidence overturns the architecture classification.
- Do not channel generally through IntVar or add domain-consistent Int semantics to Word domains.

## Done when

- [x] Unsigned/signed shift and extension dispatch handles mixed amount/value interpretations correctly.
- [x] Element, product_mod, counts, reductions, and conditional surfaces are individually retained or rejected from exact measurements.
- [x] All retained actors use conventional Word events, aliases, clone lifecycle, and cube fallback.
- [x] Structural, mixed Word/Int, and Word/Bool tests and realistic controls pass without material regression.

## Validation

- Run focused Structure, Element, ProductMod, Conditional, Channel, counts/reductions, MiniModel, and TestFramework suites.
- Run bounded and cube workload comparisons for every retained enhancement.
- Run zd check word and git diff --check.

## Result

Retained bounded numeric propagation only for non-wrapping unsigned fixed and variable left shift, with explicit distinct-view routing and established cube/proxy fallback; measured and rejected or deferred extensions, right shifts, Element, ITE, product_mod, counts, and reductions.

Validation:

- Focused Structure, Element, ProductMod, Conditional, Channel, Count, Reduction, MiniModel, and TestFramework suites; interval safety, aliases, width64, clone/replay/subsumption, exact bounded/cube benchmarks, warning build, independent verification, zd check word, and diff checks pass.
