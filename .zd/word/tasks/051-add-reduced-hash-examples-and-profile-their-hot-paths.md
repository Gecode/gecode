+++
schema_version = 1
id = "word-051"
key = "hash-examples-profile"
area = "word"
status = "done"
blocked_by = ["word-050"]
+++
# Add reduced-hash examples and profile their hot paths

## Outcome

Authentic configurable MD5-16 and SHA-1-16 examples provide a standard end-to-end profiling loop, and detailed measurements identify concrete native implementation inefficiencies suitable for narrow follow-up tasks.

## Boundaries

- Add ordinary Script examples and normal build inventories only; do not add a benchmark runner, raw results, profiling framework, hash-specific propagator, or production optimization in this task.
- Keep default example runs modest while preserving explicit 148-bit MD5 and 180-bit SHA-1 profiling configurations.
- Profile established Word and Boolean/decomposition variants only where they isolate a mechanism; do not manufacture gains with repeated roots, output, poor branching, or incomparable search.

## Done when

- [x] Two licensed, configurable ordinary examples implement authentic 32-bit MD5 and SHA-1 prefixes with concrete targets, standard cloning/search ownership, concise output, and conditional Word build integration.
- [x] Focused example builds and small runs verify concrete-oracle semantics and stable model output without making ordinary example validation take tens of seconds.
- [x] Temporary Release profiling reproduces the retained tens-of-seconds configurations and records end-to-end counters, bounded samples, clone/recomputation sensitivity, and targeted n-ary-add work/publication measurements.
- [x] Three to five ranked falsifiable hypotheses are tested one variable at a time; confirmed inefficiencies, rejected hypotheses, provenance, and exact proposed follow-up task boundaries are recorded in the Word brief.

## Validation

- Build only the two new example targets and run their modest defaults plus bounded retained profiling configurations.
- Compare exact solutions/checksums and nodes/failures whenever a decomposition or experimental implementation is measured.
- Run focused existing Word arithmetic and TestFramework checks if temporary instrumentation or model construction exposes a concrete concern, then zd check word and git diff --check.

## Result

Added and independently verified configurable reduced MD5 and SHA-1 examples with detailed provisional hot-path profiling.

Validation:

- Both focused Release targets and quick defaults passed; retained MD5/SHA-1 configurations reproduced exact solution and search counters.
- Five ranked hypotheses, provenance, limitations, and provisional follow-up boundaries were independently verified; zd check and diff check passed.
