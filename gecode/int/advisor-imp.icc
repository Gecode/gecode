/* -*- mode: C++; c-basic-offset: 2; indent-tabs-mode: nil -*- */
/*
 *  Main authors:
 *     Mikael Lagerkvist <lagerkvist@gecode.org>
 *
 *  Copyright:
 *     Mikael Lagerkvist, 2006
 *
 *  Last modified:
 *     $Date$ by $Author$
 *     $Revision$
 *
 *  This file is part of Gecode, the generic constraint
 *  development environment:
 *     http://www.gecode.org
 *
 *  See the file "LICENSE" for information on usage and
 *  redistribution of this file, and for a
 *     DISCLAIMER OF ALL WARRANTIES.
 *
 */

namespace Gecode {
  /**
   * \defgroup TaskAdvisorInt Integer advisors
   *
   * \ingroup TaskAdvisor
   */

  //@{
  namespace {
    template <class View> 
    struct ViewIntMod {
      static int mod(int val) {
        return val;
      }
      static int state_mod(int val, View) {
        return val;
      }
    };
    template <>
    struct ViewIntMod<Int::MinusView> {
      static int mod(int val) {
        return -val;
      }
      static int state_mod(int val, Int::MinusView) {
        return -val;
      }
    };
    template <>
    struct ViewIntMod<Int::OffsetView> {
      static int mod(int val) {
        GECODE_NEVER;
        return false;
      }
      static int state_mod(int val,  Int::OffsetView v) {
        return val + v.offset();
      }
    };
    template <>
    struct ViewIntMod<Int::IntScaleView> {
      static int mod(int val) {
        GECODE_NEVER;
        return false;
      }
      static int state_mod(int val,  Int::IntScaleView v) {
        return v.scale()*val;
      }
    };
    template <>
    struct ViewIntMod<Int::DoubleScaleView> {
      static int mod(int val) {
        GECODE_NEVER;
        return false;
      }
      static int state_mod(int val,  Int::DoubleScaleView v) {
        return v.scale()*val;
      }
    };
    template <>
    struct ViewIntMod<Int::NegBoolView> {
      static int mod(int val) {
        assert(val == 0 || val == 1);
        return val == 0;
      }
      static int state_mod(int val, Int::NegBoolView) {
        assert(val == 0 || val == 1);
        return val == 0;
      }
    };
  }

  forceinline int 
  IntAdvisor::_mod(int val) {
    return val;
  }

  
  /** \brief Base advisorclass for advisors that subscribe to views of type \a View. 
   */
  template <class View>
  class IntViewAdvisor : public IntAdvisor {
  public:
    IntViewAdvisor(Space* home, Propagator* p) : IntAdvisor(home,p) {}
    IntViewAdvisor(Space* home, Propagator* p, bool share, IntViewAdvisor<View>& d) 
      : IntAdvisor(home, p, share, d) {}

    virtual size_t dispose(Space* home) {
      (void) IntAdvisor::dispose(home);
      return sizeof(*this);
    }
  protected:
    /** \brief Schedule parent propagator for execution.
     *
     * The propagator is scheduled for exectuion with a ModEvent of \a
     * me.
     *
     * \warning If the propagator is currently propagating, then
     * scheduling it will not make the propagator run again.
     */
    forceinline void
    schedule(Space* home, ModEvent me) {
      View::schedule(home, parent(), me);
    }
  private:
    int _mod(int val) {
      return ViewIntMod<View>::mod(val);
    }
  };


  /** \brief Base advisorclass for unary advisors that subscribe to
   * one view (possibly carrying state).
   *
   * The primary kind of view that carries state for integer
   * variables are OffsetView and ScaleView.
   */
  template <class View>
  class IntUnaryViewAdvisor : public IntViewAdvisor<View> {
  protected:
    View x;
  public:
    IntUnaryViewAdvisor(Space* home, Propagator* p, View v) 
      : IntViewAdvisor<View>(home,p), x(v) {
      x.subscribe(home, this);
    }
    IntUnaryViewAdvisor(Space* home, Propagator* p, 
                      bool share, IntUnaryViewAdvisor<View>& d) 
      : IntViewAdvisor<View>(home, p, share, d) {
      x.update(home, share, d.x);
    }
    virtual size_t dispose(Space* home) {
      x.cancel(home, this);
      (void) IntViewAdvisor<View>::dispose(home);
      return sizeof(*this);
    }
  private:
    int _mod(int val) {
      return ViewIntMod<View>::state_mod(val, x);
    }
  };
  //@}

}

// STATISTICS: int-prop

