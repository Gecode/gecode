% CLI fixture v1: distinct x,y,z in 1..3, y=2 and x<z imply [1,2,3].
var 1..3: x;
var 1..3: y;
var 1..3: z;
array [1..3] of var 1..3: a :: output_array([1..3]) = [x,y,z];
constraint all_different_int(a);
constraint int_eq(y,2);
constraint int_lt(x,z);
solve maximize z;
